import 'package:flutter/material.dart';

class CatchUp extends StatelessWidget {
  const CatchUp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Catch up'));
  }
}
